  <h1>You can rate this restaurant: </h1>
  <%= simple_form_for([@restaurant, @review], remote: true) do |f| %>
    <%= f.input :content %>
    <%= f.input :rating, error: 'Rating not accepted' %>
    <%= f.submit %>
  <% end %>